{
  "name": "gomoku-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k -n VITE,ELECTRON -c green,blue \"vite\" \"wait-on tcp:127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 node node_modules/electron/cli.js .\"",
    "dev:vite": "vite",
    "dev:electron": "wait-on tcp:127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 node node_modules/electron/cli.js .",
    "dev:5174": "concurrently -k -n VITE,ELECTRON -c green,blue \"vite --port 5174 --strictPort\" \"wait-on tcp:127.0.0.1:5174 && cross-env VITE_DEV_SERVER_URL=http://localhost:5174 node node_modules/electron/cli.js .\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "electron ."
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "@types/node": "^22.7.5",
    "electron": "^31.4.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-electron": "^0.28.8",
    "wait-on": "^8.0.2",
    "cross-env": "^7.0.3"
  }
}
