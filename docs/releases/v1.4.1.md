# Gomoku v1.4.1 · 收束界面细节

版本 `1.4.1+10401`。这一版只打磨看起来别扭的地方：首页横幅、设置列表、棋谱缩略图、对局条和顶栏。规则、联机协议和服务端裁定都没有改。

- **首页横幅**:去掉不对称切角，改成均匀圆角的 `primaryContainer`。标题从展示级字号降到标题级，主按钮恢复普通填充按钮，不再反色抢一层。
- **设置列表**:同一组合成一张 `surfaceContainerLow` 卡片，行与行之间用分割线，不再用不同圆角拼缝。种子色选中标记改成浅色圆点加主色勾。
- **棋谱与对局**:缩略图改用棋盘色板，空状态去掉细边框。对局双方圆角固定，轮到谁只变底色。顶栏高度统一为 56。
- **设计记录**:`DESIGN.md` 同步去掉 hero notch，并更新按钮、列表组和字体说明。

验证:界面与设计令牌的静态分析无问题；布局与对局流程测试 33 项通过。这是自动测试，不是真机验收。无协议变更，无数据库迁移。六端客户端与 Debian 12 服务端构建由 v1.4.1 标签流水线执行，全部成功后才发布。

Android 使用既有正式证书，versionCode 10401，可直接覆盖安装 v1.4.0；Windows 解压后运行 `Gomoku.exe`；macOS 未公证；iOS 包未签名。

服务端行为与 v1.4.0 相同，版本号改为 1.4.1。升级使用 `manage.py install Gomoku-1.4.1-server-debian12-x64.tar.gz`（Docker 部署则更新镜像后 `docker compose up -d`）。无新增迁移，不升级也不影响现有房间与棋谱。