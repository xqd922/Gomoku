{
  "name": "gomoku-server",
  "version": "1.0.0",
  "description": "Gomoku game server for cross-network multiplayer using Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.20.2"
  }
}