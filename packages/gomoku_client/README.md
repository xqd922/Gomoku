# Gomoku client protocol

Generated Serverpod 3.4.13 models and client methods shared by Flutter and
real-service integration tests. Endpoints are `room` and `profile`.

Edit endpoint definitions and `.spy.yaml` models in `apps/server`, then run
Serverpod generation there. Do not hand-edit generated protocol code.

See [architecture and generation](../../docs/ARCHITECTURE.md).
